import styled from "styled-components";

const HeaderContainer = styled.div`
  width: 100%;
  display: flex;
  align-items: center;
  position: relative;
  justify-content: end;
`;

export default HeaderContainer;